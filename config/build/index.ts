import plugins            from './buildPlugins';
import loaders            from './buildLoaders';
import resolvers          from './buildResolvers';
import devServer          from './buildDevServer';

export default {
    resolvers,
    devServer,
    plugins,
    loaders,
};
